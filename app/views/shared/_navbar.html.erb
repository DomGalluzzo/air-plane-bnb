    <nav class="navbar navbar-expand-lg fixed-top py-3 navbar-lewagon">
          <%= link_to "/", class: "navbar-brand" do %>
            <%= show_svg('airplane.svg') %>
          <% end %>
            <button type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler navbar-toggler-right"><i class="fa fa-bars text-white"></i></button>
            
            <div id="navbarSupportedContent" class="collapse navbar-collapse">
                <ul class="navbar-nav ml-auto mr-5">
                  <% if user_signed_in? %>
                    <li class="navbar-item active"><%= link_to :logout, destroy_user_session_path, method: "delete", data: { confirm: "Are you sure? ", title: "Log Out?", commit: "Yes", cancel: "No" }, class: "nav-link text-uppercase font-weight-bold" %></li>
                  <% else %>
                    <li class="navbar-item active"><%= link_to "SIGN UP", new_user_registration_path, class: "nav-link text-uppercase font-weight-bold" %></li>
                    <li class="nav-item active"><%= link_to "Login", new_user_session_path, class: "nav-link text-uppercase font-weight-bold" %></li>
                  
                  <% end %>
                </ul>
                  <%= link_to "List your plane", new_plane_path, class: "btn btn-danger mr-5 rounded-pill" %>
            </div>
        </div>
    </nav>
